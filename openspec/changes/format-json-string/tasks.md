## 1. 既存実装の確認

- [x] 1.1 `JsonFormatter.Format` の既存実装が spec の「JSON文字列の整形出力」「スカラー値の元表記保持」要件を満たしていることを、既存の `JsonCliFormatter.Tests` を実行して確認する（`dotnet test` が成功する）
- [x] 1.2 `Program.cs` の引数解析・入出力処理が spec の各要件（入力ソースの選択、出力先の指定、インデント幅の指定、不正なJSON入力へのエラー処理）に対応していることをコードレビューで確認し、ギャップがあれば本タスクリストに追記する（レビュー結果: すべての要件に対応済みでギャップなし）

## 2. CLIレベルのテスト追加

- [x] 2.1 CLIの入力ソース（既存ファイルパスの位置引数、`-i/--input`、JSON文字列リテラルの位置引数、標準入力）を検証する統合テストを追加し、テストが成功することを確認する
- [x] 2.2 CLIの出力先（`-o/--output` 指定時のファイル出力、未指定時の標準出力）を検証するテストを追加し、テストが成功することを確認する
- [x] 2.3 `--indent` オプション（正常な非負整数、負の値、非数値）を検証するテストを追加し、不正な値でエラーメッセージが標準エラー出力へ表示され非ゼロの終了コードが返ることを確認する
- [x] 2.4 不正なJSON入力をCLI経由で渡した場合に、エラーメッセージが標準エラー出力へ表示され非ゼロの終了コードが返ることを検証するテストを追加する

## 3. ドキュメント整合性の確認

- [x] 3.1 README.md の使用例・オプション説明が実装と一致していることを確認し、不一致があれば更新する（レビュー結果: 既存の記述は実装と一致しており更新不要）

## 4. 最終検証

- [x] 4.1 `dotnet test` を実行し、全テストが成功することを確認する
- [x] 4.2 `openspec validate format-json-string --strict` を実行し、変更が有効であることを確認する
